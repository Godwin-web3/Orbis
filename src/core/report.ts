import type { Classification, MintCandidate, Opportunity, PreparedTransaction, SimulationResult } from "../domain/types";

export type CandidateReport = { candidate: MintCandidate; decision: "PASS" | "REJECT" | "SKIP"; reasons: string[]; classification: Classification; transaction?: PreparedTransaction; simulation: { success: boolean; gasEstimate?: string; stateDiffAvailable: boolean; assetDiff: SimulationResult["assetDiff"]; approvalDiff: SimulationResult["approvalDiff"]; externalValueTransfers?: SimulationResult["externalValueTransfers"]; revertReason?: string; metadata?: SimulationResult["metadata"] }; opportunity: Pick<Opportunity, "probability" | "estimatedValueNative" | "gasNative" | "executionRisk" | "expectedValueNative" | "priority">; timing: Record<string, string | number | null> };

export function buildCandidateReport(candidate: MintCandidate, classification: Classification, simulation: SimulationResult, opportunity: Opportunity, decision: { allowed: boolean; reasons: string[] }, timing: Record<string, string | number | null>, transaction?: PreparedTransaction): CandidateReport { return { candidate, decision: decision.allowed ? "PASS" : opportunity.expectedValueNative <= 0 ? "SKIP" : "REJECT", reasons: decision.reasons, classification, transaction, simulation: { success: simulation.success, gasEstimate: simulation.gasEstimate?.toString(), stateDiffAvailable: simulation.stateDiffAvailable, assetDiff: simulation.assetDiff, approvalDiff: simulation.approvalDiff, externalValueTransfers: simulation.externalValueTransfers, revertReason: simulation.revertReason, metadata: simulation.metadata }, opportunity: { probability: opportunity.probability, estimatedValueNative: opportunity.estimatedValueNative, gasNative: opportunity.gasNative, executionRisk: opportunity.executionRisk, expectedValueNative: opportunity.expectedValueNative, priority: opportunity.priority }, timing }; }
